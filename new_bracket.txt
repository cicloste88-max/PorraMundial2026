/* bracket-results.css — Porra Mundial 2026
   Vista de resultados reales del torneo (bracket acordeón)
   Prefijo: brk- en todas las clases para evitar colisiones con ko-* y br-*
   Deps: base.css (vars --bg, --card, --border, --green, --blue, --gold)
*/

/* ── ROOT ── */
.brk-root {
  background: #080b11;
  border-radius: 14px;
  overflow: hidden;
  font-family: 'Inter', system-ui, sans-serif;
}

/* ── PHASE RAIL ── */
.brk-rail {
  display: flex;
  background: #050709;
  border-bottom: 1px solid #0e1018;
  height: 38px;
  overflow-x: auto;
  scrollbar-width: none;
}
.brk-rail::-webkit-scrollbar { display: none; }

.brk-ph {
  flex: 1;
  min-width: 64px;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 5px;
  font-size: 9px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: .08em;
  cursor: pointer;
  transition: background .2s;
  white-space: nowrap;
  padding: 0 8px;
  border-bottom: 2px solid transparent;
  color: #3d4460;
}
.brk-ph.done   { color: #3ddc84; border-bottom-color: #3ddc84; }
.brk-ph.done:hover { background: #061a0e; }
.brk-ph.active { color: #4f8ef7; background: #08122a; border-bottom-color: #4f8ef7; }
.brk-ph.future:hover { background: #0a0c14; }
.brk-ph.final  { color: #fbbf24; background: #0c0900; border-bottom-color: #fbbf24; }

.brk-ph-dot {
  width: 5px; height: 5px;
  border-radius: 50%; flex-shrink: 0;
}
.brk-ph.done   .brk-ph-dot { background: #3ddc84; }
.brk-ph.active .brk-ph-dot { background: #4f8ef7; animation: brkBlink 1.4s ease-in-out infinite; }
.brk-ph.future .brk-ph-dot { background: #3d4460; }
.brk-ph.final  .brk-ph-dot { background: #fbbf24; }

@keyframes brkBlink { 0%,100%{opacity:1} 50%{opacity:.25} }

/* ── BRACKET SCROLL ── */
.brk-scroll {
  overflow-x: auto;
  -webkit-overflow-scrolling: touch;
  cursor: grab;
}
.brk-scroll:active { cursor: grabbing; }
.brk-body {
  display: flex;
  align-items: stretch;
  min-width: 860px;
}

/* ── COLUMNS ── */
.brk-col {
  display: flex;
  flex-direction: column;
  transition: flex .35s cubic-bezier(.4,0,.2,1), opacity .3s;
}
.brk-col.past    { flex: 0 0 50px; background: #06080d; opacity: .7; border-right: 1px solid #0c0e14; }
.brk-col.active  { flex: 1; background: #0d1018; border-right: 1px solid #1a1e2e; }
.brk-col.future  { flex: 0 0 36px; background: #06080d; border-right: 1px solid #090b10; }
.brk-col.f1 { opacity: .45; }
.brk-col.f2 { opacity: .25; }
.brk-col.f3 { opacity: .12; }
.brk-col.center  { flex: 0 0 130px; background: #0c0900; border-left: 1px solid rgba(251,191,36,.12); border-right: 1px solid rgba(251,191,36,.12); }
.brk-col.rpast   { border-right: none; border-left: 1px solid #0c0e14; }
.brk-col.ractive { border-right: none; border-left: 1px solid #1a1e2e; }
.brk-col.rfuture { border-right: none; border-left: 1px solid #090b10; }

/* ── COL HEADER ── */
.brk-col-hd {
  padding: 10px 8px 8px;
  border-bottom: 1px solid #0c0e14;
  flex-shrink: 0;
  text-align: center;
}
.brk-col.past  .brk-col-hd,
.brk-col.rpast .brk-col-h